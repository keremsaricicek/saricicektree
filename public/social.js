'use strict';
document.addEventListener('DOMContentLoaded',()=>{
 const previous=searchDialog;
 searchDialog=function(treeOnly=false){
  if(treeOnly||demoMode)return previous(treeOnly);
  modal('Aile arşivinde ara','<label class="search" style="width:100%"><input id="server-search" aria-label="Tüm aile arşivinde ara" placeholder="Kişi, şehir, tarih veya hatıra…" autofocus></label><p>Görmeye yetkili olduğun tüm kayıtlar ve belgeler aranır.</p><div id="server-results" role="status"></div>');
  const input=$('#server-search'),results=$('#server-results');let timer,revision=0;
  async function load(query,offset,version){try{const r=await api('/api/search?q='+encodeURIComponent(query)+'&offset='+offset);if(version!==revision||!input.isConnected)return;const html=r.items.map(x=>'<button class="search-result" data-action="'+esc(x.action)+'" data-id="'+esc(x.id)+'"><span><strong>'+esc(x.title)+'</strong><small>'+esc(x.kind)+'</small></span></button>').join('');if(!offset)results.innerHTML=html||'<p>Eşleşme bulunamadı.</p>';else results.insertAdjacentHTML('beforeend',html);if(r.hasMore){const b=document.createElement('button');b.className='btn';b.textContent='Daha fazla sonuç';b.onclick=()=>{b.remove();load(query,r.nextOffset,version);};results.append(b);}}catch(e){if(version===revision)results.textContent=e.message;}}
  input.oninput=()=>{clearTimeout(timer);const q=input.value.trim(),version=++revision;if(q.length<2){results.textContent='En az iki karakter yaz.';return;}results.textContent='Aranıyor…';timer=setTimeout(()=>load(q,0,version),300);};input.focus();
 };
 document.addEventListener('click',async e=>{const el=e.target.closest('[data-action]');if(!el)return;try{if(el.dataset.action==='search-photo'){const p=await api('/api/search/photo/'+el.dataset.id);if(!state.photos.some(x=>x.id===p.id))state.photos.push(p);el.dataset.action='photo';el.click();}if(el.dataset.action==='search-document'){const r=await fetch('/document/'+encodeURIComponent(el.dataset.id),{credentials:'same-origin',headers:{'X-Family-Factor':sessionStorage.getItem('sf-factor')||''}});if(!r.ok)throw Error('Belgeye erişilemiyor.');const url=URL.createObjectURL(await r.blob()),a=document.createElement('a');a.href=url;a.download='aile-belgesi.pdf';a.click();setTimeout(()=>URL.revokeObjectURL(url),30000);}}catch(err){toast(err.message);}});
});

async function familyPushSettings(){
 if(demoMode){modal('Telefon bildirimleri','<p>Bildirimler gerçek hesabınla canlı siteden açılır. Önizleme bildirim göndermez.</p>');return;}
 if(!('serviceWorker' in navigator)||!('PushManager' in window)){modal('Telefon bildirimleri','<p>Bu tarayıcı bildirimleri desteklemiyor. iPhone’da siteyi ana ekrana ekleyip oradan açmayı deneyebilirsin. Yerel mağaza uygulamasının bildirimi ayrıca kurulmalıdır.</p>');return;}
 modal('Telefon bildirimleri','<p>Yeni mesaj geldiğinde genel bir bildirim gösterilir. Mesajın metni ve gönderenin adı kilit ekranına yazılmaz. E-posta gönderilmez.</p><button class="btn primary" id="push-enable">Bu cihazda aç</button> <button class="btn" id="push-disable">Bu cihazda kapat</button><p id="push-state" role="status"></p>');
 $('#push-enable').onclick=async()=>{const target=$('#push-state');try{const permission=await Notification.requestPermission();if(permission!=='granted')throw Error('Bildirim izni verilmedi.');await navigator.serviceWorker.register('/sw.js');const registration=await navigator.serviceWorker.ready;const key=await api('/api/notifications/key','POST',{});const bytes=Uint8Array.from(atob(key.publicKey.replace(/-/g,'+').replace(/_/g,'/')),c=>c.charCodeAt(0));const sub=await registration.pushManager.getSubscription()||await registration.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:bytes});await api('/api/notifications/subscription','POST',{endpoint:sub.endpoint});target.textContent='Bu cihaz bildirimlere kaydedildi. Teslimat cihaz ve tarayıcı ayarlarına bağlıdır.';}catch(e){target.textContent=e.message;}};
 $('#push-disable').onclick=async()=>{try{const registration=await navigator.serviceWorker.getRegistration('/'),sub=await registration?.pushManager.getSubscription();if(sub){await api('/api/notifications/subscription','DELETE',{endpoint:sub.endpoint});await sub.unsubscribe();}$('#push-state').textContent='Bu cihazın bildirimleri kapalı.';}catch(e){$('#push-state').textContent=e.message;}};
}
document.addEventListener('click',e=>{if(e.target.closest('[data-action="push-settings"]'))familyPushSettings();});
