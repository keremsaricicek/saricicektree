import test from 'node:test';import assert from 'node:assert/strict';import vm from 'node:vm';import{readFileSync}from'node:fs';
test('world map includes real boundaries and requires no remote tiles',()=>{const context={window:{}};vm.runInNewContext(readFileSync('public/assets/world-map.js','utf8'),context);const world=context.window.FamilyWorld;assert.equal(world.type,'FeatureCollection');assert.ok(world.features.length>150);assert.ok(world.features.some(x=>x.id==='792'||x.properties.name==='Turkey'));const renderer=readFileSync('public/community.js','utf8');assert.match(renderer,/L\.geoJSON\(window\.FamilyWorld/);assert.doesNotMatch(renderer,/L\.tileLayer|tile\.openstreetmap/);const html=readFileSync('public/index.html','utf8');assert.ok(html.indexOf('assets/world-map.js')<html.indexOf('community.js'));});
